import { ActionTypes } from '../actions'

export const viz = (
  state = { currentTime: Date.now() }
, action) => {
    // console.log(action);
    switch(action.type) {
      
      default:
          return state;
    }
  }
